# Створюємо список чисел
numbers = [10, 20, 30, 40, 50]

# Обчислюємо суму всіх чисел у списку
total = sum(numbers)

# Обчислюємо кількість елементів у списку
count = len(numbers)

# Обчислюємо середнє значення
average = total / count

print(total)
print(count)

